<?xml version="1.0" encoding="utf-8"?>
<!-- Modify this file to customize your launch splash screen -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:drawable="@android:color/white" />
    <!--String pics = new String[]{"<primo></primo><secondo></secondo><terzo></terzo>};
    int randomIndex = new Random().nextInt(3);

    int resId = getResources().getIdentifier(pics[randomIndex], "string", your_package_name);

    String randomString = getString(resId);-->

    <!-- You can insert your own image assets here -->

     <item>
        <bitmap
            android:gravity="center"
            android:src="@drawable/logo_mc" />
    </item>
</layer-list>
