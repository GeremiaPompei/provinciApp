import 'package:flutter/cupertino.dart';

class EventiView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('EventiView'),
    );
  }
}
